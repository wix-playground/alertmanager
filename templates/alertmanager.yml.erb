<%= render '_global.yaml.erb' %>
route:
  receiver: 'ignore'
  routes:
<%= render '_routes.yaml.erb' %>
receivers:
- name: 'ignore'
    ## Blackhole receiver
<%= render '_receivers.yaml.erb' %>
templates:
- '/templates/*.tmpl'
